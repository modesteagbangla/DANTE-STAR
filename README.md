# DANTE-STAR
Path finding algorithm


file generator:
generates maps
use:
cd ganerator
make
./generator width (int) height (int)
folder solver:
path search
use:
cd solver
make
./solver path to map file